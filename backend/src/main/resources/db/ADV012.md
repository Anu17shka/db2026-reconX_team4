Liquibase Verification

The database migrations were successfully applied using Liquibase.

Database Connection

```bash
docker exec -it reconx-postgres psql -U reconx_user -d reconx
```

Verify Created Tables

Command:

```sql
\dt
```

Output:

```
Schema |         Name          |       Type
-------+-----------------------+-------------------
public | audit_log             | table
public | counterparties        | table
public | databasechangelog     | table
public | databasechangeloglock | table
public | instruments           | table
public | recon_breaks          | table
```

Verify Applied Liquibase Changesets

Command:

```sql
SELECT id, author, filename
FROM databasechangelog
ORDER BY orderexecuted;
```

Output:

```
id                         | author  | filename
---------------------------+---------+----------------------------------------------
001-init-tag               | trainer | db/changelog/changes/001-init.xml
002-create-counterparties  | trainer | db/changelog/changes/002-schema.xml
002-create-instruments     | trainer | db/changelog/changes/002-schema.xml
002-create-trades          | trainer | db/changelog/changes/002-schema.xml
002-create-settlements     | trainer | db/changelog/changes/002-schema.xml
003-jsonb-on-instruments   | trainer | db/changelog/changes/003-jsonb.xml
```

This confirms that the Liquibase master changelog is correctly configured and migrations are applied in dependency order.
