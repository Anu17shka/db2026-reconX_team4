Liquibase Idempotency Verification Completed ✅

1. First boot:
   - Liquibase executed changesets successfully.
   - databasechangelog contains EXECUTED entries.

2. Preconditions:
   - MARK_RAN verified:
     006-add-recon-breaks-fk-nonpartitioned → MARK_RAN

3. Second boot:
   - Liquibase output:
     "Database is up to date, no changesets to execute"
   - Run: 0
   - Previously run: 18

4. Seed idempotency:
   Before restart:
     counterparties = 10
     instruments = 15
     users = 4

   After restart:
     counterparties = 10
     instruments = 15
     users = 4

   No duplicate seed insertion observed